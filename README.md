
  # Define project requirements

  This is a code bundle for Define project requirements. The original project is available at https://www.figma.com/design/jhBXOtatL32doyp77HJmt9/Define-project-requirements.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  